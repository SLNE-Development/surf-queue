name: Publish

on:
  push:
    branches:
      - version/*
  workflow_dispatch:

jobs:
  build:
    uses: SLNE-Development/surf-workflows/.github/workflows/build-publish-release-gradle.yml@twisti-dev-patch-1
    with:
      modules: "surf-queue-paper-*-all.jar;surf-queue-velocity-*-all.jar"
    secrets: inherit
